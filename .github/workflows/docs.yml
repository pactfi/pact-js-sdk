name: Docs
on:
  push:
    tags:
      - "**"
jobs:
  Push-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
      - run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "<>"
      - name: Fetch pages
        uses: actions/checkout@v2
        with:
          repository: "pactfi/pact-js-sdk-private"
          ref: gh-pages
          path: gh-pages
          token: ${{ secrets.GH_PAT }}
      - run: npm install
      - run: ./push-docs.sh
